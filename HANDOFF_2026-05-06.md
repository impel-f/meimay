# 引き継ぎ書 2026-05-06

## 最初に読む

- 作業場所: `C:\Users\8mits\Documents\meimay-C`
- ブランチ: `main`
- 現在の `main` は `origin/main` と一致している。
- 最新コミット: `cffdd65 Polish naming flow and Android purchase setup`
- ただし、直近のUI/データ修正が未コミットで残っている。次スレッドではまずこの差分を確認して、必要なら検証後に commit / push する。
- 個人情報、APIキー、サービスアカウントJSON、RevenueCat/Googleの秘密情報はこの引き継ぎ書には残していない。必要な値はユーザーの各管理画面で確認する。

## 作業ルール

- 日本語で進める。
- 既存ファイルを直接直す。後ろに override JS を追加しない。
- 重複した top-level 関数・変数定義を作らない。
- 変更前に `git grep` / `Select-String` / `rg` で現行実装を探す。
- JS/CSS を触ったら `public/index.html` の cache busting version を合わせる。
- 変更後は `git diff --check`、JSを触った場合は `node --check`、可能なら `npm run build`。
- Capacitor反映が必要な変更後は `npm run cap:copy` または `npm run cap:sync` を実行する。
- 未関係の dirty / untracked は触らない。

## 現在のGit状態

`git status --short --branch` の概要:

- `## main...origin/main`
- 未コミットの変更:
  - `public/css/main.css`
  - `public/data/idioms.json`
  - `public/index.html`
  - `public/js/05-ui-render.js`
- 関係ない dirty / untracked:
  - `.claude/worktrees/laughing-perlman`
  - `.claude/worktrees/sweet-montalcini`
  - `antigravity-discord-bot`
  - `child_diff.txt`
  - `tmp/` 配下多数
  - `release/app-store-screenshots/` 配下複数
  - `release/google-play-listing/`

注意:

- `public/css/main.css` と `public/js/05-ui-render.js` は `git diff --check` 時に CRLF/LF 警告が出るが、直近の差分チェック自体は通っている。
- `release/google-play-listing/` はGoogle Play掲載用素材の置き場。アプリ実行には不要だが、ストア素材の再利用性を残すなら別途コミット対象にしてよい。

## 未コミットで入っている直近修正

### 1. 漢字詳細の読み・名乗り表示

対象:

- `public/js/05-ui-render.js`
- `public/css/main.css`
- `public/index.html`

内容:

- 漢字詳細モーダルの「読み・名乗り」を下側の情報エリアへ移した後、スクロール不要になったため、チップ一覧を枠いっぱいに表示する方向に調整した。
- 今選んでいる読みだけ、少し濃い背景・枠・太字で分かるようにした。
- 追加した主な関数:
  - `normalizeKanjiDetailReadingForCompare`
  - `getKanjiDetailCurrentReading`
  - `renderKanjiDetailReadingChips(readings, activeReading)`
- AI深掘り用の現在読み判定も `getKanjiDetailCurrentReading` を使う形に整理した。
- `public/index.html` の cache bust:
  - `05-ui-render.js?v=25.69`

### 2. スワイプ中カードの重なり順

対象:

- `public/css/main.css`
- `public/index.html`

内容:

- スワイプ中のカードが左右の「候補」「見送り」などの丸ボタン・枠の下に潜り込む見た目を避けるため、スワイプコンテナの `overflow` と `z-index` を調整した。
- `#main-session-content`, `#uni-session-content` を `overflow: visible` に変更。
- `#stack-container`, `#uni-swipe-container` に `overflow: visible`, `isolation: isolate`, `z-index: 1250 !important` を追加。
- `stamp-premium` の `z-index` を `1700` に上げ、スワイプ時の「候補」「見送り」「本命」表示はカードより前に出るようにした。
- `public/index.html` の cache bust:
  - `main.css?v=26.02`

未確認:

- in-app browser のスクショ取得がタイムアウトしたため、最終的な見た目は右ブラウザまたは実機で要確認。

### 3. 四字熟語データの「同上」除去

対象:

- `public/data/idioms.json`

内容:

- `（同上）` が意味欄に出ていたものを削除。
- 削除済み:
  - `宏大無辺`
  - `古今無双`
  - `醇風美俗`
  - `脣歯輔車`
- `rg -n "同上" public/data/idioms.json` は該当なし。

## 直近の検証

実行済み:

- `node --check public/js/05-ui-render.js`
- `node -e "JSON.parse(require('fs').readFileSync('public/data/idioms.json','utf8')); console.log('idioms json ok')"`
- `git diff --check`
- `npm run cap:copy`
- ローカルサーバー: `http://127.0.0.1:3000/` は 200 応答確認済み

注意:

- `git diff --check` は CRLF/LF 警告のみ。
- in-app browser のスクショ取得はタイムアウトしたが、DOM表示とローカルサーバー起動は確認済み。

## Apple / App Store Connect の状況

### 審査

- iOS 1.0 は一度提出済み。
- Appleから `Guideline 2.1 - Information Needed - New App Submission` で追加情報を求められた。
- ユーザーは App Review に以下を返信済み:
  - 実機録画のGoogle Drive URL
  - アプリ目的
  - アクセス方法
  - 外部サービス一覧
  - 地域差分なし
  - 規制業種ではない旨
- 現在はAppleの返答待ち。

### App Store素材

- スクショはアップロード済み。
- プロモーション用テキスト、概要、キーワード、サポートURL、プライバシーポリシーURLなどは一通り設定済み。
- アプリ価格は無料、アプリ内課金でプレミアムを提供する方針。
- App Storeプライバシーは公開済み。`NSUserTrackingUsageDescription` が含まれるため、AdMob関連のトラッキング開示も入っている。

### app-ads.txt / AdMob iOS

問題:

- AdMobで iOSアプリ確認時に `app-ads.txt` の情報が一致しないという警告が出ている。

確認済み:

- `public/app-ads.txt` の中身:
  - `google.com, pub-9172754377890289, DIRECT, f08c47fec0942fa0`
- `https://meimay.vercel.app/app-ads.txt` は 200、`text/plain`、内容一致。
- `public/index.html` の AdSense publisher ID も `pub-9172754377890289`。
- `public/js/14-admob.js` の AdMob app/unit ID も同じ publisher 系。
- iOS app ID:
  - `ca-app-pub-9172754377890289~1972559504`
- iOS banner unit:
  - `ca-app-pub-9172754377890289/4985735650`

残り:

- App Store Connect の iOS 1.0 画面は配信/審査状態によりグレーアウトしていて `マーケティングURL` を編集できない。
- 1.0では `サポートURL` は `https://meimay.vercel.app/support.html`、`マーケティングURL` は空欄だった。
- AdMobがまだ直らない場合は、新バージョン `1.0.1` 作成時に `マーケティングURL` へ `https://meimay.vercel.app/` を入れる。
- その後 AdMob 側で「アップデートを確認」。

## Android / Google Play の状況

### コード側

- Capacitor appId / Android package:
  - `com.impelf.meimay`
- Android app name:
  - `メイメー`
- `android/app/src/main/res/values/strings.xml`:
  - `admob_app_id`: `ca-app-pub-9172754377890289~3397321817`
- Android AdMob banner unit:
  - `ca-app-pub-9172754377890289/2546450164`
- Android build設定:
  - `versionCode` は `ANDROID_VERSION_CODE` / `PROJECT_BUILD_NUMBER` / `BUILD_NUMBER` / fallback `1`
  - `versionName` は `ANDROID_VERSION_NAME` / fallback `1.0`
- 直近のAAB:
  - `android/app/build/outputs/bundle/release/app-release.aab`
  - 作成時刻: 2026-05-06 00:23頃
  - サイズ: 約 10.5 MB

注意:

- 直近未コミット修正はこのAAB作成後の可能性がある。提出や内部テスト更新前に、最新コードでAABを作り直すこと。
- Androidのインストール済みアイコンが古い/初期アイコンに見える場合は、最新アイコンを含むAABを再ビルドして内部テストへ新リリースとして上げる。

### Google Play Console

ユーザー操作で進んでいること:

- Google Play Consoleでアプリ作成済み。
- アプリ名: `メイメー`
- package: `com.impelf.meimay`
- 内部テスト用にAABをアップロード済み。
- 内部テスト画面で「テスター未指定」の警告が出ていた。
- 内部テストのテスター設定画面で、メールリスト作成が必要。
- 商品は「1回限りのアイテム」として以下が作成済みの見込み:
  - `meimay.premium.pass.1month`
  - `meimay.premium.pass.3months`
  - `meimay.premium.lifetime`
- 1か月・3か月・買い切りの各商品に購入オプション1つずつがある状態まで進んでいた。

残り:

- 内部テスターのメールリスト作成、メール追加、保存。
- 内部テストリリースをテスターに公開。
- Google Play のストア掲載情報を完了。
- Google Play のアプリコンテンツ/データセーフティ/広告/対象年齢/コンテンツレーティング/プライバシーポリシー等を完了。
- RevenueCat と Google Play の連携を完了。
- Android実機で購入テストを通す。

## Google Play掲載情報

`release/google-play-listing/ja-JP/` に掲載用素材が生成済み。現在は untracked。

含まれている主な素材:

- `app-icon-512.png`
- `feature-graphic-1024x500.png`
- `phone-1080x1920/`
- `tablet-7-1080x1920/`
- `tablet-10-1080x1920/`

Google Playに入れる文言案:

短い説明:

```text
ふたりで読み・漢字・画数を見比べ、納得できる赤ちゃんの名前選びをサポート。
```

詳しい説明:

```text
メイメーは、赤ちゃんの名前選びをふたりで進めやすくする名づけサポートアプリです。

好きな響きをスワイプで直感的に整理し、気になる読みから使いたい漢字を探せます。集めた読みや漢字はストックでき、候補の名前を自由に組み立てて保存できます。

主な機能
・読み候補を本命、候補、見送りで整理
・読みから使いたい漢字を検索
・漢字の意味、画数、読み、名乗りを確認
・集めた漢字から名前を自由に組み立て
・姓名判断の五格や画数を参考表示
・名前候補を保存し、メモを残して比較
・パートナー連携で本命や候補を共有
・プレミアムで人名用漢字やAI深掘りなどを利用

名前は、響き、漢字、意味、画数、家族の好みなど、考えることがたくさんあります。メイメーは、候補が増えても迷子にならないように、読み探しから漢字選び、名前の組み立て、保存、パートナーとの共有までをひとつの流れで支えます。

姓名判断や画数は、名づけの参考情報として表示します。最終的な名前選びは、ご家族の想いや納得感を大切にしながら進められます。
```

フィーチャーグラフィック:

- `release/google-play-listing/ja-JP/feature-graphic-1024x500.png`

スマホスクショ:

- `release/google-play-listing/ja-JP/phone-1080x1920/` から最低2枚、推奨4枚以上。

7インチ/10インチタブレットスクショ:

- それぞれ同名の `tablet-7-1080x1920/`、`tablet-10-1080x1920/` を使う。

## RevenueCat / Google Play 課金連携

現状:

- iOS RevenueCat は動作しているが、Androidの購入完了はまだ通っていない。
- Google Play商品は作成済みだが、RevenueCat側のPlay Store設定とサービスアカウントJSON連携が未完了/要確認。

次にやること:

1. Google Play Consoleでアプリと課金商品が作成済みであることを確認。
2. Google Cloud / Play Console API連携でサービスアカウントを用意。
3. サービスアカウントにGoogle Play Console側の必要権限を付与。
4. JSONキーを発行してRevenueCatの Android / Google Play configuration にアップロード。
5. RevenueCatのProducts/Offeringsで以下の商品IDを登録・紐付け:
   - `meimay.premium.pass.1month`
   - `meimay.premium.pass.3months`
   - `meimay.premium.lifetime`
6. Entitlement は `premium`。
7. Android内部テストで購入画面まで進み、購入完了後にアプリ全体がプレミアム扱いになるか確認。

注意:

- サービスアカウントJSONやキーは絶対にリポジトリへコミットしない。
- ユーザーが画面上で Financial reports bucket ID らしき値を控えているが、この文書には残していない。RevenueCatが要求する場合のみ管理画面から再確認して入力する。
- Google Cloudの無料トライアル/請求アップグレード警告は、Play連携のためのAPI有効化時に出ることがある。課金リスクがあるため、必要性を確認してからユーザー操作で進める。

## AdMob / Android

現状:

- Android app ID / banner unit はコードに入っている。
- iOS側は app-ads.txt のクロール/URL反映待ち。
- Android側もGoogle Playアプリ公開情報が整ったら、AdMob側でアプリストア連携・確認が必要。

残り:

- Android実機/内部テストで本物のバナーまたはテストバナーが出るか確認。
- テスト中はAdMobのテストデバイス設定またはテスト広告IDを使う。
- 本番広告が出ない場合は以下を確認:
  - AdMobアプリがGoogle Playとリンク済みか
  - app-ads.txt が `https://meimay.vercel.app/app-ads.txt` で取得できるか
  - Google Play掲載のデベロッパーWebサイト/プライバシーポリシーURLと app-ads.txt ドメインが矛盾していないか
  - 広告ユニットIDが本番/テストで混ざっていないか

## 直近の既知バグ / 要確認

### コード修正済みだが実機確認が必要

- スワイプ中カードの重なり順。
- 漢字詳細の読み・名乗り表示。
- 四字熟語 `（同上）` 削除。

### まだ気にしておくこと

- 読みカードの人名例ロジック:
  - `と` の「人」
  - `うま` の「馬」
  - `す` の「壽」
  - 男の子の `こ` で「子」
  など、カード例に出さない調整が入っているか再確認。
- 同じ読みでスキップ/候補選択をまたいだ時に、ビルドやストックで漢字が重複しないこと。
- 連携時、同じ漢字を双方がいいねしてもカード数を増やさず、1枚のグラデーション表示になること。
- 苗字が人名用漢字外の場合、読みをタップした後の名前例で苗字までグレーになるバグが直っているか。
- スワイプ候補追加トーストが完了ボタンを隠しすぎないこと。
- 初回10枚後の「続けて見る」で、読み/漢字それぞれ初回演出が再表示されないこと。
- Android購入テストが完了すること。

## 次スレッドで最初にやること

1. `HANDOFF_2026-05-06.md` を読む。
2. `git status --short --branch` を確認。
3. 未コミット差分を確認:
   - `git diff -- public/css/main.css public/data/idioms.json public/index.html public/js/05-ui-render.js`
4. ローカルまたは右ブラウザで、スワイプ中の重なり順と漢字詳細モーダルを確認。
5. 問題なければ以下を実行:
   - `node --check public/js/05-ui-render.js`
   - `node -e "JSON.parse(require('fs').readFileSync('public/data/idioms.json','utf8')); console.log('idioms json ok')"`
   - `git diff --check`
   - `npm run cap:copy`
6. 未コミットの4ファイルとこの引き継ぎ書を commit / push するか確認。
7. AndroidのGoogle Play / RevenueCat / AdMob作業へ戻る。

## Android作業の次の具体手順

優先順:

1. Google Play内部テストのテスターを追加して、内部テストリリースを実際に配信できる状態にする。
2. Google Play掲載情報を埋める:
   - 短い説明
   - 詳しい説明
   - アプリアイコン
   - フィーチャーグラフィック
   - スマホ/7インチ/10インチスクショ
   - プライバシーポリシーURL: `https://meimay.vercel.app/privacy.html`
3. Google Play商品3つの価格・提供国・有効状態を確認。
4. RevenueCatにGoogle Play service account JSONを登録。
5. RevenueCatでAndroid商品をProducts/Offerings/Entitlementに紐付け。
6. 最新コードでAndroid AABを作り直す。
7. 内部テストへ新しいリリースとしてアップロード。
8. Android実機で:
   - 初回起動
   - 読みスワイプ
   - 漢字スワイプ
   - ビルド
   - 保存
   - パートナー連携
   - 広告表示
   - プレミアム購入
   - 購入後のプレミアム反映
   - アプリ削除/再インストール後の購入同期
   を確認。

## よく使うコマンド

ローカル起動:

```powershell
npx serve public -l 3000
```

検証:

```powershell
node --check public/js/05-ui-render.js
node -e "JSON.parse(require('fs').readFileSync('public/data/idioms.json','utf8')); console.log('idioms json ok')"
git diff --check
npm run build
npm run cap:copy
```

Android AAB作成:

```powershell
$env:ANDROID_VERSION_CODE="2"
$env:ANDROID_VERSION_NAME="1.0.1"
npm run build
npm run cap:copy
cd android
.\gradlew.bat bundleRelease
```

補足:

- `ANDROID_VERSION_CODE` はGoogle Playで既存より大きい値が必要。
- `ANDROID_VERSION_NAME` は表示用。iOSの `1.0.1` と揃えるなら同じにする。
- 署名設定は `android/app/build.gradle` が `key.properties` または環境変数を読む。

## 触ってはいけない/注意するもの

- サービスアカウントJSON、APIキー、RevenueCatキー、Google/Appleの個人情報はコミットしない。
- `tmp/` 配下は検証成果物が大量にある。必要なもの以外は触らない。
- `.claude/worktrees/*` と `antigravity-discord-bot` は今回作業と無関係。差分があっても無視する。
- `release/google-play-listing/` はストア素材置き場。コミットするかはユーザー確認。
