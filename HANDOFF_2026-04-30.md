# 引き継ぎ書 2026-04-30

## 作業ルール

- 日本語で進める。
- 既存ファイルを直接直す。後ろに override JS を追加しない。
- 重複した top-level 関数・変数定義を作らない。
- 変更前に `git grep` / `Select-String` / `rg` で現行実装を探す。
- JS/CSS を触ったら `public/index.html` の cache busting version を合わせる。
- 変更後は `git diff --check`、JSを触った場合は `node --check`、可能なら `npm run build`。
- 「ビルド」は「組み立て」と言い換えない。
- 小修正ごとの main push は不要。ポイントごとに push。
- 未関係の dirty / untracked は触らない。

## 現在地

- リポジトリ: `C:\Users\8mits\Documents\meimay-C`
- ブランチ: `main`
- 最新コミット: `47578b1 Add privacy policy and support contact pages`
- まだ push はしていない。
- アプリの本番 URL は `https://meimay.vercel.app/` を使う方針。`meimay.jp` は今後使わない予定。

## 触らない dirty / untracked

- `.claude/worktrees/laughing-perlman`
- `.claude/worktrees/sweet-montalcini`
- `antigravity-discord-bot`
- `PUBLIC_RELEASE_CHECKLIST.md`
- `child_diff.txt`
- `tmp/iap-review-*.png`
- `tmp/serve-3000.*.log`

## 未コミットの主な変更

### 1. AI API URL の native 対応

対象:

- `public/js/08-origin.js`
- `public/js/04-ui-flow.js`
- `public/index.html`

内容:

- TestFlight / iPad の Capacitor 環境で `fetch('/api/gemini')` が `capacitor://localhost/api/...` 扱いになり、AI漢字説明が `the string did not match the expected pattern` で失敗していた。
- 既存 helper `getMeimayApiUrl(path)` を使うように変更。
- native の場合は `https://meimay.vercel.app/api/...`、通常ブラウザでは相対 URL のまま。
- `public/index.html` の cache bust:
  - `04-ui-flow.js?v=26.22`
  - `08-origin.js?v=25.44`

検証済み:

- `git diff --check`
- `node --check public\js\08-origin.js`
- `node --check public\js\04-ui-flow.js`
- `npm run build`

注意:

- `public/js/08-origin.js` は CRLF/LF 警告が出るが、構文チェックは通っている。
- 実機確認には新しい TestFlight ビルドが必要。

### 2. 「響きで選ぶ」の初期表示ロジック

対象:

- `public/js/04-ui-flow.js`

内容:

- 初回にいきなり偏った候補へ寄せず、定番・人気の読みから傾向をつかめるようにした。
- 明治安田「名前ランキング2025 読み方ベスト50」を参照して、序盤の候補を決定的な順番に寄せる。
- その後は既存の好み反映・探索ロジックへ移る。
- 追加した主な要素:
  - `SOUND_STARTER_SOURCE_READINGS`
  - `SOUND_STARTER_OPENING_LIMIT = 18`
  - `normalizeSoundStarterReading`
  - `getSoundStarterReadingList`
  - `getSoundStarterSourceIndex`
  - `mergeSoundStarterCandidates`
  - `buildStarterReadingOrder`

意図:

- 「人気の名前やメジャーな名前を最初に出して、そこからユーザーの好みに近づける」方向。

要確認:

- 実際の候補順が自然か。
- 男の子 / 女の子 / 指定なしの序盤が偏りすぎていないか。
- かな表記候補が押せないことがある件との関係。

### 3. ストア掲載文・スクショ構成案

新規ファイル:

- `STORE_LISTING_SCREENSHOT_PLAN.md`

内容:

- App Store / Google Play の掲載文ドラフト。
- スクショ構成表。
- スクショ撮影用の画面状態案。
- アイコン・素材の方向性。
- スクショ撮影前に直すべき UI 課題。

このファイルはまだ未コミット。

## 外部サービス設定の進捗

### Apple / App Store Connect

方針:

- 1か月・3か月は自動更新サブスクではなく、非更新の期間パスにする。
- 買い切りは非消耗型。
- 解約し忘れクレームを避けるため、自動更新サブスクは使わない。

価格:

- 1か月: 480円
- 3か月: 980円
- 買い切り: 1,980円

Product ID:

- `meimay.premium.pass.1month`
- `meimay.premium.pass.3months`
- `meimay.premium.lifetime`

TestFlight:

- Codemagic 経由で iOS TestFlight ビルド成功。
- App Store Connect で `1.0 (4)` が表示されている。
- iPad に TestFlight 版を入れて購入テストまで実施済み。

### RevenueCat

設定済み:

- Bundle ID: `com.impelf.meimay`
- RevenueCat iOS public API key: `appl_iANPgUKzgQIuwcKXMrvmSKkxIhX`
- Entitlement ID: `premium`
- Offering: `default`
- Packages:
  - `pass_1month`
  - `pass_3months`
  - `lifetime`
- 対応商品:
  - `meimay.premium.pass.1month`
  - `meimay.premium.pass.3months`
  - `meimay.premium.lifetime`

Webhook:

- URL: `https://meimay.vercel.app/api/revenuecat-webhook`
- App: `All apps`
- Events: `All events`
- Environment: `Both Production and Sandbox`
- Vercel env:
  - `REVENUECAT_WEBHOOK_AUTH`
  - `REVENUECAT_ENTITLEMENT_ID`
- RevenueCat の test webhook は最初 401、認証値調整後に 200 になった。

秘密情報:

- `REVENUECAT_WEBHOOK_AUTH` の値は文書に書かない。
- Vercel / RevenueCat 側で管理。

### Codemagic

- GitHub 連携済み。
- Apple Developer Portal integration 設定済み。
- 証明書・プロビジョニング周りで一度失敗したが、code-signing 用 env を追加後に成功。
- TestFlight upload ビルドが緑になり `App.ipa` ができている。

## 実機 / TestFlight で見つかった課題

ユーザーが iPad で実際に確認した内容:

- ヘッダーが充電・電波などステータスバー領域と干渉している。
- バナー広告が出る想定だが出ない。
- スワイプの漢字をタップしたとき、読み・名乗りが右にはみ出して全部見られない。
- ことわざの読みで長いものが `...` 表示になり、全文が見られない。
- 無料体験が開始できない可能性がある。
- 無料体験まわりのトースト文が変。
- 「購入状態を確認」ボタンの意味が分かりづらい。
- ランキングが自分が選んだものしか出ないように見える。
- ランキングのハート数が多くて変。サーバー接続・集計の確認が必要。
- トーストとヒントがいつ何を出すか洗い出しが必要。
- 「響きで選ぶ」の候補順は、定番名から入り好みに近づける方向で再確認。
- 「かな表記も候補に出す」が押せないときがある。原因調査が必要。
- AI漢字説明は native API URL 修正後の新ビルドで再確認が必要。

## 次スレッドの最初のおすすめ

1. 現在の未コミット差分を確認する。

   ```powershell
   git status --short
   git diff -- public\index.html public\js\04-ui-flow.js public\js\08-origin.js
   ```

2. まず未コミット変更の検証を通す。

   ```powershell
   git diff --check
   node --check public\js\04-ui-flow.js
   node --check public\js\08-origin.js
   npm run build
   ```

3. 問題なければ、以下をまとめてコミット候補にする。

   - native の AI API URL 修正
   - 「響きで選ぶ」の定番読みスターター
   - `STORE_LISTING_SCREENSHOT_PLAN.md`
   - この `HANDOFF_2026-04-30.md`

   コミット名案:

   ```text
   Fix native AI API URLs and seed sound recommendations
   ```

4. 新しい TestFlight ビルドを出して、iPad で以下を優先確認する。

   - AI漢字説明が取得できるか。
   - 「響きで選ぶ」の初期候補順が自然か。
   - 購入状態・無料体験表示が壊れていないか。

5. 次に UI の明確な不具合を直す。

   優先度高:

   - safe-area 対応でヘッダー干渉を直す。
   - 漢字詳細の読み・名乗りの横はみ出しを直す。
   - 長い読み / ことわざ表示を全文確認できるようにする。
   - 無料体験開始とトースト文を直す。
   - ランキングの接続・表示条件を確認する。
   - かな表記候補が押せない原因を調べる。

6. スクショ撮影用の状態を整える。

   参照:

   - `STORE_LISTING_SCREENSHOT_PLAN.md`

   予定 query:

   - `?storeShot=home`
   - `?storeShot=sound`
   - `?storeShot=kanji`
   - `?storeShot=saved`
   - `?storeShot=partner`
   - `?storeShot=premium`
   - `?storeShot=ranking`
   - `?storeShot=restore`

## リリースまでにまだ必要そうなもの

- App Store / Google Play の掲載文確定。
- App Store スクショ、Google Play スクショ、必要なら feature graphic。
- アイコン最終版。
- プライバシーポリシー・お問い合わせ導線の本番確認。
- 課金導線の審査用説明・スクショ。
- RevenueCat 実購入 / Sandbox の反映確認。
- Android / Google Play 側の商品作成と RevenueCat 接続。
- 広告表示の本番確認。
- バックアップ復元 API の本番確認。
- Firestore 書き込み頻度・無料枠運用の再確認。

## 参考コミット

- `d34dc5e Fix encountered date header spacing`
- `47578b1 Add privacy policy and support contact pages`
